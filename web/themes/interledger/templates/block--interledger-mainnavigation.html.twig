{% set heading_id = attributes.id ~ '-menu'|clean_id %}

<nav class="site-nav" role="navigation" aria-labelledby="{{ heading_id }}"{{ attributes|without('role', 'aria-labelledby') }}>
  {% if not configuration.label_display %}
    {% set title_attributes = title_attributes.addClass('visually-hidden') %}
  {% endif %}
  {{ title_prefix }}
  <h2{{ title_attributes.setAttribute('id', heading_id) }}>{{ configuration.label }}</h2>
  {{ title_suffix }}

  {# node 1 = / #}
  <a class="site-logo" href={{ path('entity.node.canonical', {'node': 1}) }} data-umami-event="Site Nav - Logo">
    <img src="{{ logo_path }}" alt="Interledger Foundation" >
  </a>
  
  <div class="site-links-wrapper" data-nav-wrapper id="siteNavLinks">
    {{ content }}
    
    {# node 578 = /summit #}
    <a class="switch-link" href={{ path('entity.node.canonical', {'node': 578}) }} data-umami-event="Site Nav - Summit link">{{ 'Interledger Summit'|t }}</a>
  </div>
  <button
    type="button"
    class="site-nav__toggle"
    aria-controls="siteNavMenu"
    aria-label="Toggle Menu"
    title="Toggle Menu"
    id="siteNavToggle"
  >
    <div id="menuIcon" class="menu-icon">
      <span></span>
      <span></span>
      <span></span>
      <span></span>
    </div>
  </button>
</nav>
